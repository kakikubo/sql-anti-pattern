-- アンチパターンの弊害⑥: 要素の追加が文字列連結になり、整合性を保てない。
-- 既存のリスト末尾に ',56' を連結してアカウントを追加している。
-- 既に 56 が含まれていても重複チェックはできず、
-- 同時更新時には片方の追加が失われる（ロストアップデート）危険もある。
UPDATE Products
SET
  account_id = account_id || ',' || 56
WHERE
  product_id = 123;
