-- アンチパターンの弊害④: 特定要素の検索に正規表現が必要で、遅く壊れやすい。
-- 「アカウント 12 が担当する製品」を探すのに、カンマ区切り文字列を
-- 単語境界 \b で囲んだ正規表現で照合している。インデックスが効かず全件走査になり、
-- 区切り文字の揺れにも弱い。
SELECT
  *
FROM
  Products
WHERE
  account_id REGEXP '\\b12\\b';
